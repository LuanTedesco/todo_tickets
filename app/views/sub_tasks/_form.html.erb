<%= form_with(model: sub_task, class: "form") do |form| %>
  <% if sub_task.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(sub_task.errors.count, "error") %> prohibited this sub_task from being saved:</h4>
      <ul>
        <% sub_task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.label :date_end %>
    <%= form.datetime_field :date_end, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= form.hidden_field :ticket_id, value: @ticket.id %>
  </div>
  <div class="form-group">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
